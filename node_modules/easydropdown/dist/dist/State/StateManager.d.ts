import IActions from './Interfaces/IActions';
import IOnAction from './Interfaces/IOnAction';
import State from './State';
declare class StateManager {
    static proxyActions(state: State, injectedActions: any, onAction: IOnAction): IActions;
    private static createStateProxy(state, onAction);
    private static getPropertyDescriptorsFromValue(state, onAction);
    private static readPropertyValue(state, key);
    private static updatePropertyValue(state, key, onAction, value);
}
export default StateManager;
