import ClassNames from '../../Config/ClassNames';
import Group from '../../State/Group';
import State from '../../State/State';
declare const group: (groupState: Group, state: State, classNames: ClassNames) => string;
export default group;
